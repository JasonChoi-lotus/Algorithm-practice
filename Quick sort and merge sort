# Various sorting method

## What is Quick sort?
-an array that works by selecting a "pivot" element and partitioning the other elements into two sub-arrays

## key features

'''
def quick_sort(arr):
    if len(arr)<2: return arr
    
    pivot = arr[0]
    low, high=[], []
    
    for i in arr[1:]:
        if i < pivot: low.append(i)
        else: high.append(i)
    return quick_sort(low)+ [pivot] +quick_sort(high)

my_list=list(map(int, input().split()))
print("Before sortin': ", my_list)
print("After sortin': ", quick_sort(my_list))
'''

